# Live LLM Matrix Results

As of 2026-05-23, these results should be treated as historical/provisional unless rerun with `BEHAVIORDRAFTS_BACKEND=activegraph_adapter` in an environment with importable `activegraph==1.0.5.post2`.

Current repo state supports ActiveGraph-backed adapter wiring with documented fallback glue. Live matrix rerun is pending.
