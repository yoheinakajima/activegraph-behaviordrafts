[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "activegraph-behaviordrafts"
version = "0.1.0"
description = "Event-sourced behavior drafts for LLM self-modification"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
